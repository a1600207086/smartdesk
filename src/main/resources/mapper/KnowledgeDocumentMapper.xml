<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartdesk.knowledge.KnowledgeDocumentMapper">

    <resultMap id="documentResultMap" type="com.smartdesk.knowledge.KnowledgeDocumentEntity">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="title" column="title"/>
        <result property="sourceType" column="source_type"/>
        <result property="sourceUri" column="source_uri"/>
        <result property="status" column="status"/>
        <result property="checksum" column="checksum"/>
        <result property="embeddingModel" column="embedding_model"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="documentColumns">
        id, tenant_id, title, source_type, source_uri, status, checksum,
        embedding_model, created_by, created_at, updated_at
    </sql>

    <insert id="insert"
            parameterType="com.smartdesk.knowledge.KnowledgeDocumentEntity"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO knowledge_document (
            tenant_id, title, source_type, source_uri, status, checksum,
            embedding_model, created_by, created_at, updated_at
        )
        VALUES (
            #{tenantId}, #{title}, #{sourceType}, #{sourceUri}, #{status}, #{checksum},
            #{embeddingModel}, #{createdBy}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findById" resultMap="documentResultMap">
        SELECT
        <include refid="documentColumns"/>
        FROM knowledge_document
        WHERE id = #{id}
    </select>

    <select id="findAllByTenantId" resultMap="documentResultMap">
        SELECT
        <include refid="documentColumns"/>
        FROM knowledge_document
        WHERE tenant_id = #{tenantId}
        ORDER BY created_at DESC, id DESC
    </select>

    <update id="updateStatus">
        UPDATE knowledge_document
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <select id="countByTenantIdAndChecksum" resultType="int">
        SELECT COUNT(*)
        FROM knowledge_document
        WHERE tenant_id = #{tenantId}
          AND checksum = #{checksum}
    </select>

</mapper>